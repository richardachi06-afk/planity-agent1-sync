# Planity Login (nur über Umgebungsvariablen, NIE im Code committen)
PLANITY_EMAIL=deine@email.de
PLANITY_PASSWORD=deinPasswort

# Wird bei Fehlern (inkl. CAPTCHA/2FA-Abbruch) aufgerufen
FALLBACK_WEBHOOK=https://example.com/fallback

# Optional: Port (Render setzt PORT automatisch)
# PORT=3000
